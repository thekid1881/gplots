const APP_PREFIX = 'myapp';
const VERSION = 'v1';

export const LOCAL_STORAGE_KEYS = {
    theme: `${APP_PREFIX}_${VERSION}_theme`,
    authToken: `${APP_PREFIX}_${VERSION}_auth_token`,
    preferences: `${APP_PREFIX}_${VERSION}_preferences`,
};